# ============================================================
# Grafana — Dashboard Provider
# ============================================================
apiVersion: 1

providers:
  - name: RealEstate
    folder: RealEstate
    type: file
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards
